import type { Lang } from "./i18n";

export const DEFAULTS = {
  bg: "#f0f8ff",
  accent: "#4444ff",
  lang: "pt-BR" as Lang,
} as const;

export const REPO = "vasfvitor/days-since-x";
export const REPO_URL = `https://github.com/${REPO}`;
